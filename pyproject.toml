[project]
name = "acp-server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "acp-sdk>=0.13.0",
    "crewai>=0.134.0",
    "crewai-tools>=0.48.0",
    "nest-asyncio>=1.6.0",
    "ruff>=0.12.1",
]
